document.addEventListener("DOMContentLoaded", () => {
    const lightningStrike = document.querySelector(".lightning-strike");

    // Trigger the big lightning strike animation on page load
    lightningStrike.style.animationPlayState = "running";
});
